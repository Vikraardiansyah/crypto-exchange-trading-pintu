import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';
import colors from '../../themes/colors';

const ForwardIcon = () => {
  return <Icon name="chevron-forward-outline" size={20} color={colors.black} />;
};

export default ForwardIcon;
